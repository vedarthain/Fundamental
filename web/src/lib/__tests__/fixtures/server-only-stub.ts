// Test stub for Next's `server-only` marker package (not installed as a real
// node module — Next provides it via a bundler alias). Vitest aliases the
// `server-only` import to this empty module so server libs load under Node.
export {};
